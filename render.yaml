services:
  - type: web
    name: qa-bot
    env: python
    buildCommand: bash build.sh
    startCommand: gunicorn qa:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: GEMINI_API_KEY
        sync: false